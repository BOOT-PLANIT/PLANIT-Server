<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.planit.planit.domain.user.mapper.UserMapper">

	<resultMap id="UserMap" type="com.planit.planit.domain.user.model.UserAccount">
		<id     property="id"           column="id"/>
		<result property="uid"          column="uid"/>
		<result property="email"        column="email"/>
		<result property="displayName"  column="display_name"/>
		<result property="photoUrl"     column="photo_url"/>
		<result property="userLevel"     column="user_level" javaType="com.planit.planit.domain.user.model.UserLevel"/>
		<result property="provider"     column="provider"/>
		<result property="emailVerified" column="email_verified"/>
		<result property="createdAt"    column="created_at"/>
		<result property="lastLoginAt"  column="last_login_at"/>
	</resultMap>

	<select id="findByUid" parameterType="string" resultMap="UserMap">
		SELECT * FROM users WHERE uid = #{uid}
	</select>

	<insert id="insertUser" parameterType="com.planit.planit.domain.user.model.UserAccount">
		INSERT INTO users(uid, email, display_name, photo_url, user_level, provider, email_verified)
		VALUES (#{uid}, #{email}, #{displayName}, #{photoUrl}, #{userLevel}, #{provider}, #{emailVerified})
	</insert>

	<update id="updateLastLogin" parameterType="string">
		UPDATE users SET last_login_at = NOW() WHERE uid = #{uid}
	</update>

	<!-- 개발자용 레벨 변경 -->
	<update id="updateUserLevel" parameterType="map">
		UPDATE users
		SET user_level = #{userLevel}
		WHERE uid = #{uid}
	</update>

</mapper>
